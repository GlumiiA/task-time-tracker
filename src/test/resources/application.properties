# Datasource is provided by Testcontainers @ServiceConnection.
# Initialize DB schema from schema.sql for tests.
spring.sql.init.mode=always

# MyBatis
mybatis.configuration.map-underscore-to-camel-case=true
